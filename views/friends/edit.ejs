<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Edit Friend</title>
</head>

<body>
    <%- include('../partials/navbar.ejs') %>
    <div class="container mt-5">
        <h1 class="mb-4">Edit <%= friend.name %></h1>
        <form action="/friends/<%= friend._id %>?_method=PUT" method="POST">
            <div class="row mb-3">
                <div class="col-md-6">
                    <label for="name" class="form-label">Name:</label>
                    <input type="text" class="form-control" name="name" id="name" placeholder="Name" value="<%= friend.name %>" required minlength="2">
                </div>
                <div class="col-md-6">
                    <label for="date" class="form-label">Year we met:</label>
                    <input type="number" class="form-control" min="2003" max="2024" name="date" id="date" placeholder="Year (e.g., 2019)" value="<%= friend.date %>" required>
                </div>
            </div>
            <div class="mb-3">
                <label for="note" class="form-label">Add a note (optional):</label>
                <textarea id="note" name="note" class="form-control" placeholder="note / anything" rows="5"><%= friend.note %></textarea>
            </div>
            <div class="d-flex justify-content-start">
                <button type="submit" class="btn btn-primary me-2">Submit</button>
                <a href="/friends/<%= friend._id %>" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</body>

</html>
