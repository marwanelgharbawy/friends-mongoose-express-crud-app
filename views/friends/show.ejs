<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title><%= friend.name %></title>
</head>

<body>
    <%- include('../partials/navbar.ejs') %>
    <div class="container mt-5">
        <h1 class="mb-4"><%= friend.name %></h1>
        
        <p><strong>Met on:</strong> <%= friend.date %></p>

        <% if (friend.note) { %>
            <p><strong>Note:</strong> <%= friend.note %></p>
        <% } %>

        <div class="d-flex gap-2 mt-3">
            <a href="/friends/<%= friend._id %>/edit" class="btn btn-success">Edit</a>
            <a href="/friends" class="btn btn-secondary">Back to friends list</a>
            <form action="/friends/<%= friend._id %>?_method=DELETE" method="POST" class="d-inline">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
        </div>
    </div>
</body>

</html>
